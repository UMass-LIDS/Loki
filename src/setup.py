from setuptools import setup

setup(name='pipescaler', version='1.0', packages=['common', 'client', 'controller',
                                                  'ilp', 'load_balancer', 'protos',
                                                  'simulator', 'utils', 'worker',],)